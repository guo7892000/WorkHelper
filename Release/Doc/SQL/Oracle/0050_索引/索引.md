### 索引
```
/*增加索引*/
CREATE INDEX IDX1_T_UC_BU_BUY_CONTRACT ON T_UC_BU_BUY_CONTRACT(OPRATE_TIME) TABLESPACE UC_IDX_TBS;
/*增加唯一索引*/
CREATE UNIQUE INDEX unique_email_idx ON employees(email);
CREATE UNIQUE INDEX unique_name_idx ON employees(first_name, last_name);
/*增加唯一索引*/
ALTER TABLE employees ADD CONSTRAINT unique_email UNIQUE (email);
/*删除索引*/
DROP INDEX index_name;
```

